# Bibliothèques nécessaires pour la Phase 0 (page d'accueil).
# On ajoutera mitreattack-python et pysigma aux Phases 1 et 2,
# au moment où on s'en servira vraiment.

fastapi            # le framework web (gère les pages et l'API)
requests           # téléchargement des données ATT&CK et Sigma depuis GitHub
pyyaml             # lecture des fichiers de règles Sigma (format YAML)
uvicorn            # le serveur qui fait tourner l'appli en local
sqlmodel           # ORM : fait le lien entre Python et la base de données
jinja2             # le moteur de gabarits HTML (templates)
python-multipart   # nécessaire pour lire les formulaires HTML
pytest             # pour écrire des tests simples
